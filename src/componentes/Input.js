export const Input = ({numero}) => {
    return(
        <input 
            className='input'
            placeholder='Clique no botao abaixo para gerar sua senha'
            value={numero}
/>
    )
}